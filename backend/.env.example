# Copy to .env and adjust. Every value here has a sane default (see app/config.py)
# so the app runs with none of this set.

# SQLite (default) needs nothing. For Postgres in production:
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/screener
DATABASE_URL=sqlite:///./data/screener.db

ENVIRONMENT=development

# Comma-separated origins allowed to call the API from a browser, or "*" for any.
# Tighten this to your real frontend domain in production.
CORS_ORIGINS=*

# Zerodha Kite Connect, for live NIFTY 50 / SENSEX and live breakouts in Market view.
# From your app at https://developers.kite.trade (set its Redirect URL to
# https://YOUR-SITE/api/kite/callback). KITE_ADMIN_KEY is a password you choose;
# the "Connect Zerodha" button asks for it. Leave empty for delayed prices.
KITE_API_KEY=
KITE_API_SECRET=
KITE_ADMIN_KEY=

# Accounts (log in / sign up). The admin account is created from these at
# startup and its password kept in sync with them. AUTH_USERNAME/AUTH_PASSWORD
# still work as the admin login if ADMIN_* are unset. With none of them set the
# site is open (local development). New sign-ups need the admin's approval.
ADMIN_EMAIL=
ADMIN_PASSWORD=
SESSION_DAYS=30

# Accounts must survive redeploys: use Postgres in production (Render ->
# New -> PostgreSQL, then paste its Internal Database URL here).
# DATABASE_URL=postgresql://user:password@host:5432/dbname
