{
  "name": "deepsweep-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android/iOS shell for Deep Sweep (deepsweep.pkresearch.in) -- Capacitor wraps the live dashboard in a full-screen native WebView and adds push notifications; no web assets of its own (server.url in capacitor.config.ts points straight at production, see MOBILE_SETUP.md).",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor-firebase/messaging": "^6.3.0",
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.1",
    "firebase": "^10.14.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0",
    "typescript": "^7.0.2"
  }
}
